const initialState = {

};


const layoutReducer = (state = initialState, action) => {

    switch (action.type) {
        case "LAYOUT_ACTION":
            
            break;
        default:
            return state;
    }
};


export default layoutReducer;